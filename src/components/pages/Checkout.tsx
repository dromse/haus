import React from 'react';

type CheckoutProps = {};

export default function Checkout(props: CheckoutProps): React.JSX.Element {
  const {} = props

  return (
    <div>
      Checkout
    </div>
  );
};
